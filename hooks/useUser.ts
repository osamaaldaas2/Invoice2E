/**
 * User data hook placeholder
 * Will be implemented in Phase 1.3
 */

type UserState = {
    user: null;
    isLoading: boolean;
};

export const useUser = (): UserState => {
    // Placeholder implementation
    return {
        user: null,
        isLoading: false,
    };
};
